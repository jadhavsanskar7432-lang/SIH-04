PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/pss04_drug_supply_chain
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173

# ── Email / SMTP (Task C) ──
# For demo: use Gmail SMTP with an App Password, or Mailtrap free tier.
# If left blank, emails will be logged to console as [EMAIL STUB].
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_FROM=noreply@pss04.local
